{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Event creation</h1>
    {{ form_start(form) }}
    <div class="row">
      <div class="col-md-12">
        {{form_row(form.author)}}
      </div>
    </div>
    {{ form_end(form)}}

        <ul class="record_actions">
    <li>
        <a href="{{ path('event') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
