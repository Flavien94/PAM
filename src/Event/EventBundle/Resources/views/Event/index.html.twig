{% extends '::base.html.twig' %}
{% block stylesheets %}
  {{ parent() }}
  <link href="{{ asset('web/css/event/index.css') }}" type="text/css" rel="stylesheet"/>
{% endblock %}
{% block body -%}
  <div class="container-fluid">

    <div class="row research">
      <form class="navbar-form navbar-fixed research-form inline-form">
        <div class="center">
          <input type="search" class="input-sm form-control" placeholder="Recherche">
        </div>
        <div class="row">
          <div class="col-md-2 col-form first-col">
            <label for="beginning">Date de début</label>
            <input type="date" name="beginning">
          </div>
          <div class="col-md-2 col-form">
            <label for="end">Date de fin</label>
            <input type="date" name="end">
          </div>
        </div>
        <div class="row right">
          <button type="submit" class="btn btn-primary btn-sm research-btn">
            <span class="glyphicon glyphicon-eye-open"></span>
            Chercher
          </button>
        </div>
      </form>
    </div>

    <h1>Événements</h1>

    <!-- <ul>
      <li>
        <a href="{{ path('event_new') }}">
          Create a new entry
        </a>
      </li>
    </ul> -->

    <div class="col-md-12 result">
      <div class="row">
        <div class="col-md-1" style="background-image:url('{{ asset("images/img1.jpg") }}')"></div>
        <div class="col-md-11">
          <table class="records_list"></div>
          <div class="row">
            <thead>
              <tr>
                <th class="col-md-2">Title</th>
                <th class="col-md-6">Description</th>
                <th class="col-md-1">Datestart</th>
                <th class="col-md-1">Dateend</th>
                <th class="col-md-1">Actions</th>
              </tr>
            </thead>
          </div>
          <div class="row">
            <tbody>
              {% for entity in entities %}
                <tr>
                  <!-- <td><a href="{{ path('event_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td> -->
                  <!-- <td>{{ entity.author }}</td> -->
                  <td>{{ entity.title }}</td>
                  <td>{{ entity.description }}</td>
                  <td>
                    {% if entity.dateStart %}{{ entity.dateStart|date('Y-m-d H:i:s') }}{% endif %}
                  </td>
                  <td>
                    {% if entity.dateEnd %}{{ entity.dateEnd|date('Y-m-d H:i:s') }}{% endif %}
                  </td>
                  <td>
                    <ul>
                      <li>
                        <a href="{{ path('event_show', { 'id': entity.id }) }}">show</a>
                      </li>
                      <li>
                        <a href="{{ path('event_edit', { 'id': entity.id }) }}">edit</a>
                      </li>
                    </ul>
                  </td>
                </tr>
              {% endfor %}
            </div>
          </table>
        </div>
        <div class="col-md-12 result">
          <div class="row">
            <div class="col-md-1" style="background-image:url('{{ asset("images/img1.jpg") }}')"></div>
            <div class="col-md-11">
              <table class="records_list"></div>
              <div class="row">
                <thead>
                  <tr>
                    <th class="col-md-2">Title</th>
                    <th class="col-md-6">Description</th>
                    <th class="col-md-1">Datestart</th>
                    <th class="col-md-1">Dateend</th>
                    <th class="col-md-1">Actions</th>
                  </tr>
                </thead>
              </div>
              <div class="row">
                <tbody>
                  {% for entity in entities %}
                    <tr>
                      <!-- <td><a href="{{ path('event_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td> -->
                      <!-- <td>{{ entity.author }}</td> -->
                      <td>{{ entity.title }}</td>
                      <td>{{ entity.description }}</td>
                      <td>
                        {% if entity.dateStart %}{{ entity.dateStart|date('Y-m-d H:i:s') }}{% endif %}
                      </td>
                      <td>
                        {% if entity.dateEnd %}{{ entity.dateEnd|date('Y-m-d H:i:s') }}{% endif %}
                      </td>
                      <td>
                        <ul>
                          <li>
                            <a href="{{ path('event_show', { 'id': entity.id }) }}">show</a>
                          </li>
                          <li>
                            <a href="{{ path('event_edit', { 'id': entity.id }) }}">edit</a>
                          </li>
                        </ul>
                      </td>
                    </tr>
                  {% endfor %}
                </div>
              </table>
            </div>

            <!-- <table class="records_list">
          <thead>
            <tr>
              <th>Id</th>
              <th>Author</th>
              <th>Title</th>
              <th>Description</th>
              <th>Contact_name</th>
              <th>Contact_email</th>
              <th>Place</th>
              <th>Datestart</th>
              <th>Dateend</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            {% for entity in entities %}
              <tr>
                <td><a href="{{ path('event_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.author }}</td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.contactname }}</td>
                <td>{{ entity.contactemail }}</td>
                <td>{{ entity.place }}</td>
                <td>
                  {% if entity.dateStart %}{{ entity.dateStart|date('Y-m-d H:i:s') }}{% endif %}
                </td>
                <td>
                  {% if entity.dateEnd %}{{ entity.dateEnd|date('Y-m-d H:i:s') }}{% endif %}
                </td>
                <td>
                  <ul>
                    <li>
                      <a href="{{ path('event_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                      <a href="{{ path('event_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                  </ul>
                </td>
              </tr>
            {% endfor %}
          </tbody>
        </table> -->
          </div>
        </div>

      {% endblock %}
